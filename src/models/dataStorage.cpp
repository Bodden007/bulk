#include "../includes/dataStorage.hpp"

std::string dataStorage::dataBulk(std::string &strBalk)
{
    if (strBalk  != " ")
    {
        std::cout << strBalk << std::endl;
    }
    else
    {
        std::cout << N << std::endl;
    }

    return std::string();
}
